[
	{
		"slug": "attendance_day_context",
		"title": "Attendance Day Context",
		"type": "Imported",
		"enabled": 1,
		"requires_confirmation": 0,
		"import_path": "flow.flow_hr.tools.attendance.attendance_day_context",
		"code": null,
		"summary": "Date-by-date attendance, leave, punches and gaps for an employee.",
		"description": "Report, date by date, what attendance already exists for an employee and what is missing — before creating any Attendance Request or regularization.\n\nPass `employee` (an ID like ST-EMP-00026, never a person's name), `from_date`, and optionally `to_date` (defaults to from_date).\n\nFor each date returns: the weekday, whether it is a holiday or week off, any Attendance record already marked (with its status and docstatus), any Leave Application covering it, any Attendance Request already raised for it, the Employee Checkin punches with their times, whether the day is a mispunch (exactly one punch), whether it is an unexplained gap, and any task deadlines falling that day.\n\n`summary` names the dates that matter: `unaccounted_dates` (no attendance, no leave, no request, not a holiday), `mispunch_dates`, `already_marked`, `on_leave`, `holidays`, and `dates_beyond_backdated_window` — dates too far in the past for HR Settings to allow a backdated Attendance Request.\n\nAlso returns the shift covering the window and its timings. Read-only: nothing is created or changed."
	},
	{
		"slug": "interview_feedback_context",
		"title": "Interview Feedback Context",
		"type": "Imported",
		"enabled": 1,
		"requires_confirmation": 0,
		"import_path": "flow.flow_hr.tools.interview_feedback.interview_feedback_context",
		"code": null,
		"summary": null,
		"description": "Everything around one interview except the transcript: the rubric, the candidate, the panel, and the feedback records already waiting.\n\nCall this first. It returns:\n  - `rubric.skills`: the exact and complete list of skills this round is scored on, each with its own `rating_scale` (they differ per skill) and what to look for. This is the whole list -- the Interview Feedback form does not allow rows to be added, so a skill that is not here cannot be rated at all.\n  - `applicant`: who is being interviewed, their designation and the opening\n  - `panel`: the interviewers, with the user each feedback record belongs to\n  - `feedback_records`: the Interview Feedback already on this interview -- one draft per interviewer is created automatically when the panel is set, so drafts existing is normal. Each says whether it is Draft or Submitted, and which skills the interviewer has already rated themselves.\n  - `write_mode`, `require_evidence`, `unrated_skill_policy`: the rules your write will be held to.\n  - `guards`: blocking problems. If this list is not empty, do what it says and stop.\n\nRead-only."
	},
	{
		"slug": "interview_transcript",
		"title": "Interview Transcript",
		"type": "Imported",
		"enabled": 1,
		"requires_confirmation": 0,
		"import_path": "flow.flow_hr.tools.interview_feedback.interview_transcript",
		"code": null,
		"summary": null,
		"description": "Fetch the transcript of one interview, normalised, whoever recorded it.\n\nWalks the transcript sources configured in Interview Feedback Agent Settings in order and returns the first one that yields dialogue, so you do not need to know -- or ask -- which tool recorded this interview. WebVTT, SRT, JSON exports and plain text all come back in the same shape.\n\nReturns `found`, an `attempts` list saying what was tried and what each source gave back, and when something was found a `transcript` carrying:\n  - `source` and `format`: where it came from and how it was read\n  - `speakers`: each voice with its `role` (Interviewer, Candidate, or null when it could not be established), turn count and word count\n  - `text`: the transcript as `[time] Speaker [Role]: what they said` lines\n  - `total_chars`, `offset`, `next_offset`: call again with `offset = next_offset` to read the rest. A long interview arrives in several windows and the later ones matter most.\n  - `usable`: false when the transcript is too short to assess anyone on\n  - `warnings`: read these. They tell you when a role could not be established.\n\nOnly lines from a speaker whose role is Candidate are evidence about the candidate. If `found` is false, there is nothing to assess -- report the attempts and stop."
	},
	{
		"slug": "leave_impact_report",
		"title": "Leave Impact Report",
		"type": "Imported",
		"enabled": 1,
		"requires_confirmation": 0,
		"import_path": "flow.flow_hr.tools.attendance.leave_impact_report",
		"code": null,
		"summary": "Full dependency check for a leave request: blockers, task and project conflicts, team coverage, balance.",
		"description": "Assemble the complete dependency picture for one leave request, in a single call, without changing anything.\n\nPass `leave_application` when reacting to a filed application (e.g. HR-LAP-2026-00040) — everything else is read off the record. For a what-if check on leave nobody has filed yet, pass `employee` (an ID like ST-EMP-00026, never a person's name) plus `from_date`, `to_date` and `leave_type` instead.\n\nReturns, all cross-checked against this install's own rules and data:\n- `verdict`: \"clean\", \"needs_review\" or \"blocked\", and `recommendation`, one line telling you what to do.\n- `predicted_blockers`: things that would make the save or submit be REFUSED — no leave allocation, insufficient balance, over the leave type's continuous-day cap, missing required attachment, too little prior notice, notice period, a Leave Block List date, an overlapping application, dates before joining. Each carries the exact reason.\n- `concerns`: real conflicts a human must weigh — milestones and high-priority tasks falling due inside the leave or just after the return, project deadlines in the window, teammates already on leave over the same dates, thin team coverage, probation.\n- `notes`: background only. Never hold a request over a note.\n- `work_dependencies`: the employee's open Tasks with their deadlines, split into due-in-window / due-soon-after / already-overdue, which are milestones, which are high priority, how far along each is, the Projects they own, and `suggested_project_details_rows` — ready-made rows for the Leave Application's custom_project_details table so each project manager can sign off on their own row.\n- `team_coverage`: how many people would be left, day by day, for the immediate team (same reporting manager) and for the department.\n- `leave_balance`: balance before, days requested, balance after, computed by this install's own calculation.\n- `notify`: the employee's email, the reporting manager, the leave approver, and every affected project manager's email.\n\nNothing is created, updated or submitted, and no decision is made for you. Report the figures and messages exactly as returned; never reword a blocker."
	},
	{
		"slug": "preview_salary_slip",
		"title": "Preview Salary Slip",
		"type": "Imported",
		"enabled": 1,
		"requires_confirmation": 0,
		"import_path": "flow.flow_hr.tools.payroll.preview_salary_slip",
		"code": null,
		"summary": null,
		"description": "Compute an employee's salary for one month using HRMS's own payroll engine, WITHOUT creating any record. Pass the Employee ID (e.g. ST-EMP-00045, never a person's name) and a posting_date inside the month you want (e.g. \"2026-08-31\" for August 2026); the salary structure is taken from the employee's submitted Salary Structure Assignment unless you pass one.\n\nReturns every earning and deduction with amounts, gross_pay, total_deduction, net_pay, rounded_total, total_working_days and payment_days, plus income_tax_slab, tax_scaffolding_complete, tax_scaffolding_issues and income_tax_note.\n\nNothing is saved: no Salary Slip is created and no data changes. Report the returned figures exactly as given. Never present a tax figure without checking tax_scaffolding_complete first."
	},
	{
		"slug": "record_interview_feedback",
		"title": "Record Interview Feedback",
		"type": "Imported",
		"enabled": 1,
		"requires_confirmation": 1,
		"import_path": "flow.flow_hr.tools.interview_feedback.record_interview_feedback",
		"code": null,
		"summary": null,
		"description": "Record your assessment of one interview. Call this exactly once, at the end.\n\nPass `summary` (the written assessment, plain prose), `ratings` (one entry per skill you are rating: skill, rating_given, evidence), `unrated` (rubric skills the interview never covered) and `recommendation` ('Cleared', 'Rejected' or 'Inconclusive').\n\nWhere it lands is configuration, not your decision: either a comment on the Interview, or the interviewers' existing draft Interview Feedback records. Either way it is a DRAFT. This tool never submits anything and never sets the result on a record -- the interviewer confirms their own verdict.\n\nIt refuses the whole write, without saving anything, if a skill is not on the rubric, a rating is outside 0..rating_scale for that skill, a skill is rated twice, or a rating arrives without evidence quoted from the transcript. The error names exactly what was wrong; fix that one thing rather than retrying the same call.\n\nA rubric skill the interview never touched belongs in `unrated`, not in `ratings` with a 0. Zero means the candidate failed at it; unrated means nobody asked.\n\nIf nothing at all could be rated -- the roles could not be established, the transcript is unusable, none of the rubric came up -- still call this once with `ratings` empty, every rubric skill listed in `unrated`, the reason in `summary`, and recommendation 'Inconclusive'. That is a real outcome and it belongs on the Interview where a human will see it, not only in this run's log. An empty call with nothing in `unrated` either is refused."
	}
]
